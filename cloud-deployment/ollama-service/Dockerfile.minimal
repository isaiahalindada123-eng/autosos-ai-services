FROM ollama/ollama:latest

# Install minimal dependencies
RUN apt-get update && apt-get install -y \
    curl \
    && rm -rf /var/lib/apt/lists/*

# Create directory for models
RUN mkdir -p /root/.ollama/models

# Copy minimal model installation script
COPY install-models-minimal.sh /usr/local/bin/
RUN chmod +x /usr/local/bin/install-models-minimal.sh

# Expose port
EXPOSE 11434

# Health check
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
    CMD curl -f http://localhost:11434/api/tags || exit 1

# Start Ollama and install minimal models
CMD ["sh", "-c", "/usr/local/bin/install-models-minimal.sh && /bin/ollama serve"]
